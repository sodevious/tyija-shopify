<!-- Renderless: see FooterMenu.vue -->
<footer-menu>
  <template v-slot="{ toggleFooter, isFooterOpen, isMenuOpen }">
    <footer 
      :class="[{ 'is-open': isFooterOpen }, 'tyija-overlay tyija-footer flex flex-col  md:px-36 md:flex md:justify-between  lg:px-40']"
     
    >
      <button class="pr-6 text-white h-header w-full flex items-center justify-end  md:hidden " 
              @click="$store.dispatch('overlays/toggleFooter')">
        <svg class="w-5 h-5">
          <use xlink:href="#close_info"></use>
        </svg>
      </button>

      <ul class="grid grid-cols-2 gap-4 p-4 items-center text-center flex-1 justify-around  md:flex md:items-start md:text-left text-white md:light:text-gray  index:text-white md:index:text-black"
          data-barba-prevent="all">
        <li>
          <h4 class="text-md uppercase mb-2 md:text-h4">{{ linklists.info.title }}</h4>

          {% for link in linklists.info.links %}
            <a href="{{ link.url }}" class="block text-h5 uppercase">{{ link.title }}</a>
          {% endfor %}
        </li>
      
        <li>
          <h4 class="text-md uppercase mb-2 md:text-h4">{{ linklists.customers.title }}</h4>

          {% for link in linklists.customers.links %}
            <a href="{{ link.url }}" class="block text-h5 uppercase">{{ link.title }}</a>
          {% endfor %}
        </li>

        <li>
          <h4 class="text-md uppercase mb-2 md:text-h4">{{ linklists.social.title }}</h4>

          {% for link in linklists.social.links %}
            <a href="{{ link.url }}" class="block text-h5 uppercase">{{ link.title }}</a>
          {% endfor %}
        </li>

        <li>
          <h4 class="text-md uppercase mb-2 md:text-h4">{{ linklists.legal.title }}</h4>

          {% for link in linklists.legal.links %}
            <a href="{{ link.url }}" class="block text-h5 uppercase">{{ link.title }}</a>
          {% endfor %}
        </li>

        <li class="col-span-2 flex flex-col items-center  md:hidden">
          <button 
            class="button button-sm my-2 light:bg-white light:mix-blend-screen light:text-gray" 
            @click="$store.dispatch('overlays/toggleSearch')">
            {{ 'action.search' | t }}
          </button>

          <a href="{{ routes.account_login_url }}"
              type="submit" class="button button-sm my-2 light:bg-white light:mix-blend-screen light:text-gray index:text-black">
            {{ 'action.sign_in' | t }}
          </a>
      
          <a href="/pages/wishlist/">
            <svg class="w-16 h-16 my-2">
              <use xlink:href="#liked"></use>
            </svg>
          </a>
        </li>
        
        <li class="lg:w-80 col-span-2">
          <h4 class="text-md uppercase mb-2 hidden md:text-h4 md:block">Sign Up</h4>

          <form class="md:flex">
            <input
              type="email"
              class="w-full text-h4 md:text-h5"
              placeholder="you@email.com"
            >
            <input type="submit" value="Submit" class="p-2 uppercase text-h4">
          </form>
        </li>
      </ul>

      <p class="p-4 uppercase text-h5 text-white md:hidden">
        &copy; {{ 'common.copyright' | t }}
      </p>
    </footer>

    <aside :class="[ isMenuOpen ? 'text-white' : 'light:text-gray index:text-black']" class="absolute">
      <button
        class="fixed z-50 bottom-9 right-10"
        @click="toggleFooter"
      >
        <svg class="w-12 h-12">
            <use xlink:href="#info_button"></use>
        </svg>
      </button>
    </aside>
  </template>
</footer-menu>

<component is="style" class="invisible">
  .tyija-footer {
    background-image: url('{{ section.settings.gradient_light | image_url: width: 1500 }}');
  }
</component>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "image_picker",
      "id": "gradient_light",
      "label": "Footer Gradient - Light"
    },
    {
      "type": "image_picker",
      "id": "gradient_dark",
      "label": "Footer Gradient - Dark"
    }
  ]
}
{% endschema %}
